// This app has no real multi-user auth — a single shared account name is
// shown in the sidebar and recorded on every activity-log entry. Import from
// here so the two can never drift apart.
export const CURRENT_USER_NAME = 'admin.warehouse';
export const CURRENT_USER_ROLE = 'Warehouse manager';
